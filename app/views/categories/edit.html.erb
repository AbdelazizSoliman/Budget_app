<div>
      <ul>
        <li><%= link_to image_tag('backarrow.png', alt: 'Back'), category_path(@category) %></li>
        <h2><strong>Editing category</strong></h2>
        <li><%= link_to 'Sign Out', destroy_user_session_path, data: { turbo_method: :delete } %></li>
      </ul>
    </div>

<%= render "form", category: @category %>
